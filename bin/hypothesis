#!/bin/sh

set -eu

cd "$(dirname "$0")"
cd ..

export PYTHONPATH="${PWD}:${PYTHONPATH:-}"
export PATH="${PWD}/bin:${PATH:-}"

if [ ! -z "${USE_PYTHON3+yes}" ]; then
    PYTHON_BIN=python3
else
    PYTHON_BIN=python
fi

exec $PYTHON_BIN -m h "$@"
